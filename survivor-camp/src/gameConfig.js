'use strict';

module.exports = Object.freeze({
  campPhaseSeconds: 30,
  tugOfWar: Object.freeze({
    durationSeconds: 12,
    playerPullPerInput: 6.2,
    opponentPullPerSecond: 17,
    opponentWaveAmplitude: 2.5,
    winThreshold: 100
  }),
  tribalCouncil: Object.freeze({
    durationSeconds: 30
  })
});
